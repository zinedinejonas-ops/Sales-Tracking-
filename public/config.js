// Configuration file for the frontend
// You can change the API base URL here if the backend is hosted separately
// If left empty or undefined, it uses the same host as the frontend (relative path)

window.CONFIG = {
  // Example: 'http://192.168.1.100:3000'
  API_BASE_URL: '' 
};
